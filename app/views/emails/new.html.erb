<h1>New Page</h1>
<%= link_to('Back to Home', {controller: 'main', action: 'index'}) %><br />

<div>
    <h2>Create a Email</h2>

    <%= form_for(@email) do |e| %>

        <%= render(partial: 'form', locals: {e: e}) %>

        <div>
            <%= e.submit("Create Email")%>
        </div>

    <% end %>
</div>

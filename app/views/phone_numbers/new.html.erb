<h1>New Page</h1>
<%= link_to('Back to Home', {controller: 'main', action: 'index'}) %><br />

<div>
    <h2>Create a Phone Number</h2>
    <h4>Person ID corresponds to the Person table</h4>
    <%= form_for(@phone_number) do |p| %>

        <%= render(partial: 'form', locals: {p: p}) %>

        <div>
            <%= p.submit("Create Phone Number")%>
        </div>

    <% end %>
</div>
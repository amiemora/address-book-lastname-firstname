<h1>Show Page</h1>
<%= link_to('Back to Person List', {controller: 'people', action: 'index'}) %><br />

<div>
    <h2><%= @person.first_name  %> <%= @person.last_name %></h2>

    <div>
 
    </div>

        <table class="table table-striped">
            <tr>
            <th>Salutation</th>
            <td><%= @person.salutation %></td>
            </tr>
            <tr>
            <th>First Name</th>
            <td><%= @person.first_name %></td>
            </tr>
            <tr>
            <th>Middle Name</th>
            <td><%= @person.middle_name %></td>
            </tr>
            <tr>
            <th>Last Name</th>
            <td><%= @person.last_name %></td>
            </tr>
            <tr>
            <th>SSN</th>
            <td><%= @person.ssn %></td>
            </tr>
            <tr>
            <th>DOB</th>
            <td><%= @person.dob %></td>
            </tr>
            <tr>
            <th>Comment</th>
            <td><%= @person.comment %></td>
            </tr>
        </table>

</div>